# Indexing Module

Owns keyword, vector, metadata, structure, and reference indexes.

PDF2GPU RAG code is a reference for Qdrant, embeddings, and reranking.

Current MVP implementation:

- builds deterministic keyword, vector, structure, and reference graph reports
- does not compute embeddings yet; vector report records configured backend/model

