# Corpus Module

Owns corpus records and folder import jobs.

Future public methods are defined in `docs/master/MASTER_INTERFACES.md`.

Initial responsibilities:

- create corpus
- scan local folder or uploaded folder
- detect duplicate source files
- emit import report

