# Regression Fixtures

Golden fixtures live here. Each fixture folder should contain:

- input document or input JSON
- expected output JSON
- expected warnings
- README with scenario description

The fixture output must follow the master documents in `docs/master`.

